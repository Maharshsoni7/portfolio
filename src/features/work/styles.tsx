import { StyleSheet } from "react-native";

export default StyleSheet.create({
    components: {
        flex: 1,
        flexDirection: 'row',
    },
    flatListComponentStyle: {
        paddingBottom: 100
    }
})